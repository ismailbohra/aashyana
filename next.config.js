/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["ismailbohra.github.io","avatars.githubusercontent.com"],
  },
};

module.exports = nextConfig;
